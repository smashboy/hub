import { createTheme } from "@mui/material"

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#00B2FF",
    },
    background: {
      paper: "#000",
    },
  },
})

export default darkTheme
